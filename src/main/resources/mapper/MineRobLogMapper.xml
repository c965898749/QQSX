<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sy.mapper.game.MineRobLogMapper">
    <select id="selectAll" resultType="com.sy.model.game.MineRobLog">
        SELECT *,ur.nickname,ur.game_img FROM
            mine_rob_log mrl
                LEFT JOIN `user` ur on ur.userId=mrl.attacker_user_id
        WHERE mrl.target_user_id=#{userId}
    </select>
</mapper>